define(function(require) {
	var $ = require("jquery"),
		_ = require("underscore"),
		Backbone = require("backbone");
	
	var JSaw
	
	var init = function() {
		// Stuff here
	};
	
	return {
		initialize: init
	};
});
